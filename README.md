# my-portfolio

Minimal Next.js (App Router) + TypeScript scaffold with ESLint + Prettier.

Quick start:

```bash
# install dependencies
npm install

# run dev server
npm run dev
```

Linting & formatting:

```bash
npm run lint
npm run type-check
npm run format
```
